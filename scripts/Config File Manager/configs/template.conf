# Configuration Template for {{ENVIRONMENT}} Environment
# Generated automatically - do not edit manually

# Database Configuration
database.host={{DB_HOST}}
database.port={{DB_PORT}}
database.name=myapp_{{ENVIRONMENT}}
database.user={{DB_USER}}
database.password={{DB_PASSWORD}}
database.max_connections={{DB_MAX_CONN}}

# Application Settings
app.name=MyApplication
app.version=1.2.3
app.environment={{ENVIRONMENT}}
app.debug={{DEBUG_MODE}}
app.log_level={{LOG_LEVEL}}
app.port={{APP_PORT}}
app.max_memory={{MAX_MEMORY}}
app.timeout={{APP_TIMEOUT}}

# Cache Configuration
cache.enabled={{CACHE_ENABLED}}
cache.type={{CACHE_TYPE}}
cache.host={{CACHE_HOST}}
cache.port={{CACHE_PORT}}
cache.ttl={{CACHE_TTL}}

# Security Settings
security.encryption={{SECURITY_ENCRYPTION}}
security.token_expiry={{TOKEN_EXPIRY}}
security.ssl_enabled={{SSL_ENABLED}}
security.api_key={{API_KEY}}

# Monitoring and Logging
log.file={{LOG_FILE}}
log.max_size={{LOG_MAX_SIZE}}
monitoring.enabled={{MONITORING_ENABLED}}
monitoring.endpoint={{MONITORING_ENDPOINT}}

# Performance Tuning
performance.worker_threads={{WORKER_THREADS}}
performance.gc_interval={{GC_INTERVAL}}
performance.enable_caching={{ENABLE_CACHING}}